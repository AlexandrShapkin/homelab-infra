dependencies:
  - role: docker
  - role: postgres
    vars:
      db_create: true
      db_list:
        - name: "{{ gitea.db_name }}"
          user: "{{ gitea.db_user }}"
          pass: "{{ gitea_db_pass }}"
  

